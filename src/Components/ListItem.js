import React, {Fragment} from 'react';


/**
 * построение списков
 * @param {*} param0 
 * @returns 
 */
function ListItem({children}) {
    return <li>
            {children}
           </li>
}

export default ListItem;